# Copy to run_config.env and edit. run_config.env is gitignored.
# Backend: isaac (submit job to UT ISAAC cluster) or local (run on this machine)
RUN_BACKEND=isaac

# ISAAC connection (used when RUN_BACKEND=isaac)
# Get host from OIT: https://oit.utk.edu/hpsc/isaac-open-enclave-new-kpb/
ISAAC_HOST=login.isaac.utk.edu
ISAAC_USER=amcclu12
ISAAC_REMOTE_DIR=~/llm_training

# SLURM partition and account (check OIT "Running Jobs" for your cluster)
ISAAC_PARTITION=campus-gpu
ISAAC_ACCOUNT=ACF-UTK0011
ISAAC_TIME=04:00:00
ISAAC_GPU_COUNT=1

# Optional: path to exported.json (default: exported.json in llm_training dir)
# EXPORTED_JSON=./exported.json

# For weekly_export_and_train.sh: Supabase (get service role key from Dashboard → Settings → API)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
