# SpeechGradebook local development config
# Copy this to .env and fill in your values:
#   cp .env.example .env
# Then run: ./run_local.sh

# Required for Supabase auth and data
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Optional: CORS (default allows localhost)
# ALLOWED_ORIGINS=http://localhost:8000,https://speechgradebook.onrender.com

# Optional: Qwen2.5-VL service for rubric extraction and video analysis
# Run: python llm_training/qwen_serve.py --port 8001
# QWEN_API_URL=http://localhost:8001

# Optional: Notify Super Admin when a new (non-invited) signup request is submitted. No SMTP needed.
# Create an Incoming Webhook in Slack and paste the URL here. See docs/SIGNUP_APPROVAL_AND_SLACK.md.
# SLACK_SIGNUP_WEBHOOK_URL=https://hooks.slack.com/services/...

# Optional: Required for "Submit Mistral/Qwen training to ISAAC" in Platform Analytics → LLM Export.
# Set to any secret string; enter the same value in the app's "API secret" field when submitting.
# RENDER_LLM_EXPORT_SECRET=your-secret-here

# ISAAC (for Submit to ISAAC - set these on Render Environment; run_config.env is gitignored)
# ISAAC_USER=your-ut-netid
# ISAAC_HOST=login.isaac.utk.edu
# ISAAC_REMOTE_DIR=~/llm_training
# ISAAC_SSH_PRIVATE_KEY=<paste full private key contents including BEGIN/END lines>
